{% extends 'base.html.twig' %}

{% block title %}Profil{% endblock %}

{% block body %}

    <div class="container my-5">
        <div class="row justify-content-center">
            <div class="col-md-6">

                <h2 class="text-center mb-4">Mon profil</h2>

                <button class="bg-secondary p-2 font-weight-bold my-auto border-0">
                    <a href="{{ path('orders') }}" class="text-white p-2 font-weight-bold text-decoration-none">Commandes</a>    
                </button>
                <button class="bg-secondary p-2 font-weight-bold my-auto border-0">
                    <a href="{{ path('profil_modify') }}" class="text-white p-2 font-weight-bold text-decoration-none">Modifier mon profil</a>    
                </button>

            </div>
        </div>
    </div>

{% endblock %}